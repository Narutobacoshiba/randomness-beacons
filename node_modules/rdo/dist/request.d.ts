/**
 * Ky interface to the Random.org v2 JSON RPC API.
*/
export declare function reqAPI(method: string, { data, getRandomData, signed }: {
    data?: object;
    getRandomData?: boolean;
    signed?: boolean;
}): any;
export declare function reqPublic(method: string, { data, convertToNumber }: {
    data?: object;
    convertToNumber?: false;
}): Promise<string[]>;
export declare function reqPublic(method: string, { data, convertToNumber }: {
    data?: object;
    convertToNumber?: true;
}): Promise<number[]>;
